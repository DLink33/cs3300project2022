<!DOCTYPE html>
<html lang="en">

  <style>
    .center {
      margin: auto;
      display: flex;
      justify-content: center;
    }

    .flex-container {
      height: 100%;
      padding: 20;
      margin: 20;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .row {
      width: auto;
      border: 3px;
      border-style: solid;
      border-color: white;
      padding: 10px
      margin: 10px
      text-align: center;
      background-color: #212529;
    }

    .flex-item {
      background-color: #212529;
      padding: 20px;
      width: 20px;
      height: 40px;
      margin: 10px;
      line-height: 20px;
      color: white;
      font-weight: bold;
      text-align: center;
      font-size: 30px;
    }

  </style>

  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/js/bootstrap.bundle.min.js%22%3E"></script>
    <title>CS3300 Portfolio Project</title>
  </head>
  
  <body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
      <div class="container-fluid">
      <a class="navbar-brand" href="/projects">Home</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="collapsibleNavbar">
        <ul class="navbar-nav">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">Account</a>
          <ul class="dropdown-menu">
            <li><a class="dropdown-item" href="#">Sign In</a></li>
            <li><a class="dropdown-item" href="#">Sign Up</a></li>
          </ul>
        </li>
        </ul>
      </div>
      </div>
    </nav>

    <div class="container-fluid">
    <div class = "row"> 
      <div class="flex-item"> CS 3300 Portfolio Project: </div>

        <% if user_signed_in? %>
          
          <div class="flex-item"> Welcome <%= current_user.email %> </div>
          <%= button_to "Sign out", destroy_user_session_path, method: :delete %> <%# not sure how to edit the style of this button %>
        
        <% else %>
          <div class="flex-item"><a href="/users/sign_in" styel="color:red" > Sign in</a></div>
        <% end %>
    </div>
  </body>
</html>